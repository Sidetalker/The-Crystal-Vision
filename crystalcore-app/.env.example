# Copy to .env (git-ignored) and fill in. Never commit real keys.
# Docs: https://docs.x.ai  ·  Keys: https://console.x.ai

# Required for SpaceXAI chat (opt-in provider only)
XAI_API_KEY=

# Optional default chat backend: ollama | spacexai
# CRYSTAL_PROVIDER=ollama
